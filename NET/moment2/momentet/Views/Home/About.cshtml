@model List<momentet.Models.CourseModel>

@{
    ViewData["Title"] = "Om mig";
    ViewData["Heading"] = "Om mig";
}

<p>Jag är en student, här är 3 slumpmässiga kurser jag läst</p>

@if(Model != null) {
    @foreach(var course in Model) {
        <li>@course.Name</li>
    }
 //Försök hämta en slumpmässigkurs
}